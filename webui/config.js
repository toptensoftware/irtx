export let config = {
    appName: "irtx",
    description: "irtx IR Blaster",
    // In production the UI is served from the device (same-origin), so deviceUrl should be "".
    // During development, point to the device IP.
    deviceUrl: "http://10.1.1.101",
}